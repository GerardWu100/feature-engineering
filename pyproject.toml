[project]
name = "feature-engineering"
version = "0.1.0"
description = "Simple categorized feature engineering pipeline for stock OHLCV data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "clickhouse-connect>=0.13.0",
    "matplotlib>=3.11.1",
    "numpy>=2",
    "pandas>=3.0.1",
    "pyarrow>=23.0.1",
    "python-dotenv>=1.2.1",
    "scipy>=1.17.1",
    "statsmodels>=0.14.6",
]

[project.scripts]
feature-pipeline = "feature_engineering.cli:main"

[build-system]
requires = ["setuptools>=82.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = false

[tool.setuptools.packages.find]
where = ["src"]
include = [
    "feature_engineering*",
]
exclude = [
    "outputs*",
    "*.outputs*",
    "*.models*",
]

[tool.uv]
package = true

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pytest>=9.0.2",
    "ruff>=0.15.6",
]
